
export interface ContactFormState {
    name: string;
    phone: string;
    category: string;
    message: string;
}

export const CONTACT_CATEGORIES = ['Konsultasi Sistem', 'Lapor Error', 'Ajak Kerjasama', 'Sekadar Sapa'];
